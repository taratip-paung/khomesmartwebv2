{
  "name": "beconnected-solar-server",
  "private": true,
  "version": "0.3.0",
  "description": "Solar Builder backend (CT 1330): Google Solar API proxy + daily cap, climate cache (NASA POWER + PVGIS); auth/projects come in S5",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "migrate": "node src/migrate.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "alert:test": "node src/alertTest.mjs"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "fastify": "^5.12.5",
    "pg": "^8.23.0"
  }
}
